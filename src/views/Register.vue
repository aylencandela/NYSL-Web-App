<template>
  <div class="register">
    <Navbar viewname="register"></Navbar>
    <div>
      <b-form @submit="onSubmit" @reset="onReset" v-if="show">
        <b-form-group
          id="input-group-1"
          label="Name:"
          label-for="input-1"
        >
          <b-form-input
            id="input-1"
            v-model="form.name"
            type="input"
            required
            placeholder="Enter your name"
          ></b-form-input>
        </b-form-group>

        <b-form-group
          id="input-group-2"
          label="Last name:"
          label-for="input-2"
        >
          <b-form-input
            id="input-2"
            v-model="form.lastname"
            type="input"
            required
            placeholder="Enter your last name"
          ></b-form-input>
        </b-form-group>

        <b-form-group id="input-group-3" label="Street Adress:" label-for="input-3">
          <b-form-input
            id="input-3"
            v-model="form.adress"
            required
            placeholder="Enter adress"
          ></b-form-input>
        </b-form-group>

        <b-form-group id="input-group-4" label="City:" label-for="input-4">
          <b-form-select
            id="input-4"
            v-model="form.city"
            required
            placeholder="Enter adress"
          ></b-form-select>
        </b-form-group>

        <b-form-group id="input-group-5" label="Zip code:" label-for="input-5">
          <b-form-select
            id="input-5"
            v-model="form.zip"
            required
            placeholder="Enter zip code"
          ></b-form-select>
        </b-form-group>

        <b-form-group id="input-group-6" label="Birth date (mm/dd/yyyy):" label-for="input-6">
          <b-form-select
            id="input-6"
            v-model="form.birth"
            required
            placeholder="Enter birth date"
          ></b-form-select>
        </b-form-group>

        <b-form-group id="input-group-4">
          <b-form-checkbox-group v-model="form.checked" id="checkboxes-4">
            <b-form-checkbox value="me">Check me out</b-form-checkbox>
            <b-form-checkbox value="that">Check that out</b-form-checkbox>
          </b-form-checkbox-group>
        </b-form-group>

        <b-button type="submit" variant="primary">Submit</b-button>
        <b-button type="reset" variant="danger">Reset</b-button>
      </b-form>
      <b-card class="mt-3" header="Form Data Result">
        <pre class="m-0">{{ form }}</pre>
      </b-card>
    </div>
  </div>
</template>

<script>
import Navbar from "../components/navbar.vue";

export default {
  name: 'Register',
  components: {
    Navbar
  },
  data: function() {
    return {
      form: {
        email: '',
        name: '',
        food: null,
        checked: []
      },
      foods: [{ text: 'Select One', value: null }, 'Carrots', 'Beans', 'Tomatoes', 'Corn'],
      show: true
    }
  },
  methods: {
    onSubmit(evt) {
      evt.preventDefault()
      alert(JSON.stringify(this.form))
    },
    onReset(evt) {
      evt.preventDefault()
      // Reset our form values
      this.form.email = ''
      this.form.name = ''
      this.form.food = null
      this.form.checked = []
      // Trick to reset/clear native browser form validation state
      this.show = false
      this.$nextTick(() => {
        this.show = true
      })
    }
  }
}
</script>

<style lang="scss" scoped>

</style>